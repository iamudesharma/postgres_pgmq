include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
  errors:
    missing_required_param: error
    missing_return: error
  exclude:
    # Separate Flutter package with its own analysis (see flutter-demo job).
    - example/pgmq_flutter_demo/**

linter:
  rules:
    - prefer_single_quotes
